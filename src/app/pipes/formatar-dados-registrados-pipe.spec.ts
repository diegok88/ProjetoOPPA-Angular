import { FormatarDadosRegistradosPipe } from './formatar-dados-registrados-pipe';

describe('FormatarDadosRegistradosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatarDadosRegistradosPipe();
    expect(pipe).toBeTruthy();
  });
});
