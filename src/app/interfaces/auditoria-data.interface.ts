export interface AuditoriaData {
  id: string;
  entidade: string;
  registroId: string;
  acao: string;
  dadosRegistrados: string;
  dataHora: Date;
  registradoPorId: string;
  empresaId: string;
}
